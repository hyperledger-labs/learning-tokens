# build output
dist/
.astro/

# dependencies
node_modules/

# cloudflare — the preview is personal infrastructure, run from a local machine.
# wrangler.jsonc carries an individual account_id; the project deploys via Pages.
.wrangler/
wrangler.jsonc

# Design source material, kept out of the repo. The Gerzso paintings are
# third-party copyrighted works (the /brand page states they are not reproduced
# here); the deck is CC BY-NC-ND, which does not sit well inside an Apache-2.0
# repository; and the pre-vector logo rasters are superseded by the traced
# masters in public/brand/. What the site ships lives in public/.
docs/

# env & misc
.env
.env.production
.DS_Store
npm-debug.log*
